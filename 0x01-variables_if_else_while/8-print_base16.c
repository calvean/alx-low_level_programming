#include <stdio.h>
/**
 * main - Prints numbers to base 16
 *
 * Return: 0 if sucess
 */
int main(void)
{
	int ch;
	int n;

	for (n = 48; n < 57; n++)
		putchar(n);
	for (ch = 'a'; ch <= f; ch++)
		putchar(ch);
	putchar(10);
	return (0);
}
